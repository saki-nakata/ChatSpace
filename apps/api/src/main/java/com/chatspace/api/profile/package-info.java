/**
 * プロフィール編集(表示名・ステータス・アバター画像URL更新)。フェーズ9(S-14)着手時に実装。
 *
 * <p>フェーズ7の時点で「プロフィール編集エンドポイントが実装計画のどのバックエンドフェーズにも含まれていない」 既知のギャップとして記録されていた({@code
 * 実装計画書/phase7.md}参照)ものを、フロントエンドのS-14モーダル 実装に合わせてここで解消する。{@code user}パッケージ・{@code
 * message}パッケージの双方に依存するが、 本パッケージ自体には誰も依存しないため循環依存は生じない。
 */
package com.chatspace.api.profile;
